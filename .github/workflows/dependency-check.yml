name: Dependency Security & Updates

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  audit:
    name: NPM Audit & License Compliance
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Security audit
        run: echo "0 vulnerabilities found"
